#include<stdio.h>
int main()
{
        int f[50],p,i,st,len,n,index[50];
        for(i=0;i<50;i++)
                f[i] = 0;
        
        prinf("Enter the index block\n");
        scanf("%d",&p);
        
        if(f[p] == 0)
        {
                printf("Enter the number of index block\n");
                scanf("%d",&n);
                for(i=0;i<n;i++)
                {
                        scanf("%d",&index[i]);
                        f[index[i]] = 1;
                }
                for(i=0;i<n;i++)
                {
                        if(index[i] == )
                        {
                                printf("Block 
                        }
                }
        }
}
